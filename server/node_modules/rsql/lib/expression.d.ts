import { IStringer, Operator } from './types';
export declare class Expression implements IStringer {
    private field;
    private operator;
    private value;
    constructor(field: string, operator: Operator, value: any);
    string: () => string;
}
